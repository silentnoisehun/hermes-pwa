{
  "name": "hermes-pwa",
  "version": "0.1.0",
  "description": "HERMES Serverless — Local-first WASM agent with Telegram bot sync",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.10.0"
  }
}
